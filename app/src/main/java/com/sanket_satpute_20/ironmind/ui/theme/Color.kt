package com.sanket_satpute_20.ironmind.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Background & Surface ───────────────────────────────────────
val DeepBackground = Color(0xFF0D0D12)      // True deep dark background per spec
val SurfaceDark = Color(0xFF16161E)         // Card / surface layer
val SurfaceGlass = Color(0x1AFFFFFF)        // 10% White — glassmorphism
val SurfaceGlassDark = Color(0x0AFFFFFF)    // 4% White — glassmorphism (deep)
val SurfaceElevated = Color(0xFF1E1E28)     // Slightly raised surface

// ─── Primary & Secondary Accents ────────────────────────────────
val NeonCyan = Color(0xFF00E5FF)            // Electric-blue secondary / success
val NeonCyanMuted = Color(0x3300E5FF)       // 20% Cyan for backgrounds
val IronRed = Color(0xFFFF4500)             // Iron-Red primary accent (urgent/destructive)
val IronRedMuted = Color(0x33FF4500)        // 20% IronRed for tinted surfaces
val ElectricViolet = Color(0xFF8A2BE2)      // Tertiary accent

// ─── Gamification Tokens ────────────────────────────────────────
val GoldXP = Color(0xFFFFD700)              // XP / rewards / streaks
val GoldXPMuted = Color(0x33FFD700)         // 20% Gold for badge backgrounds
val SuccessGreen = Color(0xFF00FF87)        // Task completion
val ErrorRed = Color(0xFFFF2A55)            // Errors (distinct from IronRed)
val WarningAmber = Color(0xFFFFB020)        // Warnings

// ─── Text Hierarchy ─────────────────────────────────────────────
val TextPrimary = Color(0xFFFFFFFF)         // 100% White
val TextSecondary = Color(0xB3FFFFFF)       // 70% White
val TextMuted = Color(0x66FFFFFF)           // 40% White
val TextDisabled = Color(0x33FFFFFF)        // 20% White

// ─── Dividers & Borders ─────────────────────────────────────────
val DividerColor = Color(0x1AFFFFFF)        // 10% White
val BorderGlass = Color(0x33FFFFFF)         // 20% White — glass card borders
